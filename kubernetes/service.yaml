apiVersion: v1
kind: Service
metadata:
  name: go-web-app-service
  labels:
    app: go-web-app
spec:
  type: ClusterIP
  selector:
    labels:
      app: go-web-app
  ports:
    - port: 80
      containerPort: 8080
